# Untagged References

These line-pinned references have a tag that isn't a valid SHA.

The handler is at [src/services/handler.ts:42:notatag](src/services/handler.ts:42:notatag).

Another one: [src/lib/parser.ts:10-20:also-not-a-sha](src/lib/parser.ts:10-20:also-not-a-sha).

Whole-file with bad tag: [src/app/page.tsx:0:fake](src/app/page.tsx:0:fake).
